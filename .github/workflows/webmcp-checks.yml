name: WebMCP checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  public-tools:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python3 scripts/build-webmcp-catalog.py --check
      - run: python3 -m unittest discover -s tests -p 'test_*.py'
      - run: node --check js/webmcp.js
      - run: node --test tests/webmcp.test.cjs
